import TodoList from ".components.TodoList";
import TodoFooter from ".components.TodoFooter";

main view App() -> {
  <div>
    <header>
      <h1>{"todos"}</h1>
      <input
        placeholder="What needs to be done?"
        autoFocus={true}
      />
      // <input(.newTodo)
      //   // value={$formValue}
      //   // onChange={(value) -> $formValue = value}
      // />
    </header>
    // {content}
    {true ? <TodoFooter /> : <span />}
  </div>
}
