view Greeting(name: string) -> "Hello, " + name + "!";

main view App(name: string) ->
  <div>
    <Greeting name />
  </div>;

state SimpleState {
  value: string = "abc";
  other = $value;

  mut setValue() -> {
    "def"
  }
}

view OtherView ~ SimpleState -> {
  <div>{$value + "def"}<button onClick={$setValue}>{"wooooah"}</button></div>
}
