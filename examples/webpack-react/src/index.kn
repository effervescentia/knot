// import JSX from "@knot/jsx";
// import App from ".components.App";
import { excite } from ".utils";

// func render(id: string) -> JSX.render(<App name="World" />, id);

func greet(name: string) -> "Bello, " + excite(name);
