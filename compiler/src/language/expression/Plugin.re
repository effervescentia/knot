include Interface;

let parse = Parser.parse;

let analyze = Analyzer.analyze;

let format = Formatter.format;
let attribute_needs_wrapper = Formatter.attribute_needs_wrapper;

let to_xml = Debug.to_xml;
