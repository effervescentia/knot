open Kore;
open Onyx;

module Character = Constants.Character;

let back_slash = char(Character.back_slash);
let eol = char(Character.eol);
let quote = char(Character.quote);
let period = char(Character.period);
let octothorpe = char(Character.octothorpe);
let underscore = char(Character.underscore);
let dollar_sign = char(Character.dollar_sign);
let at_sign = char(Character.at_sign);
