# `module/invalidate`

## Parameters

- `path` (*string*): absolute path to the module

## Example

```json
{
  "jsonrpc": "2.0",
  "id": 99,
  "method": "module/invalidate",
  "params": {
    "path": "/path/to/file.kn"
  }
}
```
