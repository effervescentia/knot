# `module/update`

## Parameters

- `path` (*string*): absolute path to the module

## Example

```json
{
  "jsonrpc": "2.0",
  "method": "module/update",
  "params": {
    "path": "/path/to/file.kn"
  }
}
```
