open Parse.Onyx;

module C = Knot.Constants.Character;

let backslash = char(C.backslash);
let eol = char(C.eol);
let quote = char(C.quote);
let period = char(C.period);
let underscore = char(C.underscore);
let semicolon = char(C.semicolon);
let equal_sign = char(C.equal_sign);
