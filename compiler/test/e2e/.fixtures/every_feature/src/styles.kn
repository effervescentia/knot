const emptyStyle = style {};

const redStyle = style {
  mockColor: $mockRed,
};

const yellowStyle = style {
  mockBackgroundColor: "#dac33d",
};

view Component -> nil;

const EmptyComponent = Component::emptyStyle;
const RedAndYellowComponent = Component::redStyle::yellowStyle;

const StyleLiteralComponent = Component::{
  mockColor: "#adec22",
};

view DynamicStyling ->
  <div::redStyle>
    <div::yellowStyle />
    <div::{ mockColor: "#adec22" } />
  </div>
;
