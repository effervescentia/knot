import greet from ".components.App";
import * as Constants from ".constants";
import * as Functions from ".functions"
import * as JSX from ".jsx";

const ABC = <div>{greet("World")}</div>;

/*
this is a comment
*/

view SimpleView -> (
  <div>
    {ABC}
    {Constants.numericConst}
  </div>
);
