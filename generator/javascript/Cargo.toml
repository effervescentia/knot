[package]
name = "knot_generator_javascript"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
kore = { package = "knot_core", path = "../../toolchain/kore", default-features = false }
lang = { package = "knot_language", path = "../../toolchain/language" }

[dev-dependencies]
insta = "1.42.1"

[features]
format = ["kore/format"]
