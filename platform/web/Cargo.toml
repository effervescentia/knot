[package]
name = "knot_platform_web"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
kore = { package = "knot_core", path = "../../toolchain/kore", default-features = false }
lang = { package = "knot_language", path = "../../toolchain/language" }
js = { package = "knot_generator_javascript", path = "../../generator/javascript" }
